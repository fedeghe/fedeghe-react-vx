

const GRIND_ITEM_NOT_SET = Symbol('grid layout will use default Item'),
    NO_DATA = Symbol('provided data is empty'),
    EMPTY_GROUP = Symbol('group named %groupName% is empty, thus will be ignored'),
    OUTKAST_DATA = Symbol('%num% some data does not belong to any group (%num%)');
// eslint-disable-next-line one-var
export default {
    GRIND_ITEM_NOT_SET,
    NO_DATA,
    EMPTY_GROUP,
    OUTKAST_DATA
};